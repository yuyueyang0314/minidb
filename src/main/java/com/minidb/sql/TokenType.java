package com.minidb.sql;
public enum TokenType {
    IDENT, NUMBER, STRING, COMMA, LPAREN, RPAREN, STAR,
    CREATE, TABLE, INSERT, INTO, VALUES, SELECT, FROM, WHERE,
    INT, TEXT, EQ, LT, GT, SEMI, EOF
}
